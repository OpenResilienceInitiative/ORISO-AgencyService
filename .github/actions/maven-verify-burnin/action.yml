name: Reusable full integration-test run

# Runs the full `verify` lifecycle so the surefire `integration-tests` execution
# (bound to the integration-test phase, includes **/*IT.*) actually runs.
#
# The calling jobs block on this suite. `-fae` keeps the report set complete, so
# a single failure does not hide the rest.

inputs:
  java_version:
    required: true

runs:
  using: 'composite'
  steps:
  - name: Set up JDK and Maven
    uses: actions/setup-java@v5
    with:
      java-version: ${{ inputs.java_version }}
      distribution: 'temurin'
      cache: maven

  - name: Run Maven verify
    id: legacy_verify
    shell: bash
    # -fae (fail-at-end) lets every module/execution run so the report set is
    # complete. Maven's exit code is not suppressed, so the step and with it
    # the calling job go red on a failure.
    run: ./mvnw -B -fae verify

  - name: Record the Maven outcome
    if: ${{ always() }}
    shell: bash
    env:
      LEGACY_VERIFY_OUTCOME: ${{ steps.legacy_verify.outcome }}
    run: |
      echo "Integration outcome: ${LEGACY_VERIFY_OUTCOME}" >> "$GITHUB_STEP_SUMMARY"

  - name: Reject incomplete test reports
    if: ${{ always() }}
    shell: bash
    # The shared guard treats zero reports, zero executed tests, failures, errors
    # and skips as red, so a vacuously green run cannot pass as a pass.
    run: |
      shopt -s nullglob globstar
      reports=( **/target/surefire-reports/TEST-*.xml **/target/failsafe-reports/TEST-*.xml )
      python3 scripts/ci/verify-test-reports.py "${reports[@]}"

  - name: Summarise integration-test results
    if: ${{ always() }}
    shell: bash
    run: |
      python3 - <<'PY'
      from pathlib import Path
      import xml.etree.ElementTree as ET

      total_tests = total_failures = total_errors = total_skipped = 0
      failing = []
      for report in Path(".").glob("**/target/surefire-reports/TEST-*.xml"):
          try:
              root = ET.parse(report).getroot()
          except ET.ParseError:
              continue
          tests = int(root.attrib.get("tests", 0))
          failures = int(root.attrib.get("failures", 0))
          errors = int(root.attrib.get("errors", 0))
          skipped = int(root.attrib.get("skipped", 0))
          total_tests += tests
          total_failures += failures
          total_errors += errors
          total_skipped += skipped
          if failures or errors:
              failing.append((report, failures, errors))

      print(f"Integration suite: tests={total_tests} "
            f"failures={total_failures} errors={total_errors} skipped={total_skipped}")
      if failing:
          print("Reports with failures/errors:")
          for report, failures, errors in sorted(failing):
              print(f"- {report}: failures={failures}, errors={errors}")
      PY

  - name: Upload test reports
    if: ${{ always() }}
    uses: actions/upload-artifact@v4
    with:
      name: integration-test-reports-${{ inputs.java_version }}
      path: |
        **/target/surefire-reports/*.xml
        **/target/failsafe-reports/*.xml
      if-no-files-found: warn
      retention-days: 7
